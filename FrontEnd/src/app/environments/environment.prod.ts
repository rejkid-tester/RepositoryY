export const environment = {
  production: true,
  apiUrl: 'http://desktop-r4q0muc:5217/api' // when you release your app to production, this should change to whatever host or address your API will be deployed to
};
